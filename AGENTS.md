## 语言规则
- 沟通用中文
- 所有输出（包括思考/推理、过程性说明、工具调用前后的文字、解释、变更说明）一律使用中文，禁止英文句子或段落
- 代码、文件名、命令、变量、状态码（如 413、EPERM、systemError）可保留原文，但**首次出现时必须用中文解释其含义**，不得整句套用英文说明
- 专有名词（如 React、nginx）按原文保留；名词周围的解释性文字仍须为中文
## 代码修改
- 理解/熟悉项目之后就要形成记忆保存本地，不要每打开一个新会话又重新开始查看项目上目录结构和项目结构
- 从记忆里快速读取
- 改后说明变更内容；不写不要求功能
- 每次改动都要用**说明清楚改了什么**（清晰的 commit message）
- 改动的代码以好维护为主
- 如果提出要求按什么思想修改，那么在修改的过程中如果存在可能的缺陷或有可能的业务需要而想添加这种业务的实现代码逻辑，则需要确认
- 若实现功能过程中被动中断了，要记录好进度相关明细信息，以便开新窗口继续任务
## 安全红线
- 若需求是要改代码，请理解后直接执行修改代码
- 密码、密钥、API KEY、token **绝不写进代码里**
- 本项目下的所有文件均可以修改，若有必要修改本项目以外的文件，必须停下来，得到我确认才能执行
- 禁止密钥硬编码、上传本地配置；删除库/批量删文件/操作账号必须停下来，得到我确认才能执行
- **以下高风险操作，必须先停下来，得到我确认以后再执行**
	- 删除 / 批量删除文件，删除数据库
	- 修改外部接口，上传数据
	- 操作真实账号
- 不执行 git 提供
## 验证要求
- 必须经验证通过后，才算修复好
- 命令报错、测试失败，需要如实反馈原因
- 修 bug：先写一个**能复现问题的测试**，再改代码
- 改完之后： **能跑测试就跑测试，能跑检查就跑检查**
- 测试失败如实说明； 修But先写复现测试
## 个人习惯
- 称呼：主人；
- 熟悉任务直接做，敏感操作先确认
- 安全的小事可以直接做，**不用每一步都经我确认**
- 请遇到熟悉的任务，**先用已装好的 Skill**，不要从零乱试
- 改完代码默认路测试，不可有低级语法错误，不做多余的“发散”
- 但凡涉及**删除、密钥、上传、外部账号**，必须**先得到我确认再动手**
